__pycache__/
*.py[cod]
.venv/
venv/
.env
# Data files (keep repository lightweight)
# General CSV ignored, BUT the data/ folder (3-year Gold M1) IS committed
# so AI agents have permanent access on every deploy.
*.csv
!data/
!data/*.csv
uploads/
/home/user/uploads/

# ======== PRIVACY: yeh files kabhi GitHub pe NAHI jaayengi ========
# tokens wali config
config.json
config.local.json
# AAPKA SECRET TRADING LOGIC
my_logic.py

# ======== EXTRA SECURITY & SECRETS PROTECTION ========
*.env*
credentials*
*.pem
*.key
*.token
*.secret
